{
  "name": "clauseguard-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd ClauseGuard-frontend && npm run dev",
    "dev:backend": "cd ClauseGuard-backend && source venv/bin/activate && uvicorn app.main:app --reload",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "install:all": "cd ClauseGuard-frontend && npm install && cd ../ClauseGuard-backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
